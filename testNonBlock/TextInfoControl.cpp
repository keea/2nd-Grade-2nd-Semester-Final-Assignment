#include "TextInfoControl.h"

void TextInfoControl::Add(GameText gameText)
{

}

TYPE TextInfoControl::ControlText(char Text[20], DWORD userId)
{
	if (texts.size() >= 0) { //처음이니까 저장한다.

	}
	return TYPE();
}
